import { shallowMount } from '@vue/test-utils'
import StageTicket from 'StageTicket';

describe('StageTicket', () => {
  it('test', () => {
    expect(1 + 1).toBe(2)
  });
  it('passed test', () => {
    expect(1 + 1).toBe(3)
  });
})
